#!/bin/bash
# Simple YouTube to MP4 downloader script using yt-dlp

if [ -z "$1" ]; then
    echo "Usage: ytmp4 <YouTube URL>"
    exit 1
fi

echo "Downloading video from: $1"
yt-dlp -f "best[ext=mp4]" -o "~/Downloads/%(title)s.%(ext)s" "$1"

echo "Download complete! File saved in ~/Downloads/"
